# Firebase Configuration for Backend
# Get your project ID from Firebase console
FIREBASE_PROJECT_ID=your_project_id

# Security Configuration
# DANGER: This bypasses JWT signature verification and should NEVER be enabled in production!
# Only set to 'true' during local development with mock tokens
SKIP_TOKEN_SIGNATURE_VERIFICATION=false

# Database Configuration
DATABASE_URL=your_database_url_here